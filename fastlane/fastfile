platform :android do
    desc "Deploy android"

    #build
    lane :build do
        ionic(
            platform: 'android',
            keystore_path: "./appkey.jks",
            keystore_alias: 'appkey',
            keystore_password: 'chleorjs12@'
        )

      


    end


    #test(firebase 테스트 앱 전달)
    lane :distribute do
        sh("sh android.sh")

        sh("pwd")

    end


    #deploy
    lane :deploy do

    end
end