import { TimerExpressionPipe } from './timer-expression.pipe';

describe('TimerExpressionPipe', () => {
  it('create an instance', () => {
    const pipe = new TimerExpressionPipe();
    expect(pipe).toBeTruthy();
  });
});
